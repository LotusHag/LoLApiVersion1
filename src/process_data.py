# src/process_data.py

def process_match_data(raw_data):
    """
    Process raw match data from the Riot API response.
    Store the entire data without filtering to keep all data points intact.
    """
    # Return the full data exactly as received from the Riot API
    return raw_data
